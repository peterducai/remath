pub mod matrix3d {
    pub struct AlgStruct {
        pub set: String,
        pub operations: Vec<String>,
        pub axioms: Vec<String>,
    }

    pub fn remath_matrix_3d(rows: i64, columns:i64) {
        println!("The value of x is: {rows} {columns}");
    }
}
